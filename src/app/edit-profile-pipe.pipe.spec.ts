import { EditProfilePipePipe } from './edit-profile-pipe.pipe';

describe('EditProfilePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EditProfilePipePipe();
    expect(pipe).toBeTruthy();
  });
});
